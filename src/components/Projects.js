import React from 'react'

const Projects = (props) => {


    return (
        <div>
            <p>{props.name.name}</p>
            <p>{props.time.time}</p>
        </div>
    )
}
export default Projects